<resources>
    <!--
        One theme for both light and dark. Material3 DayNight adapts on its own;
        the values-night variant only overrides the brand colours.

        Since Android 16 (API 36) apps are always edge-to-edge and cannot opt out,
        so the system bars are transparent and MainActivity insets the content
        instead.
    -->
    <style name="Theme.EnergyDrink" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>
</resources>
